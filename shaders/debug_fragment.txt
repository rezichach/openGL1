#version 330 core
out vec4 color;

in vec2 fragmentTexCoord;

uniform sampler2D imageTexture;

void main()
{
    float depthValue = texture(imageTexture, fragmentTexCoord).r;
    color = vec4(vec3(depthValue), 1.0);
} 